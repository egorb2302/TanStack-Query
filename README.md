# TanStack Query + React Router v7
This project is writing with using:
- React (TypeScript)
- TanStack Query
- React Router v7
- Vite

## For run project
```
cd "App"
npm run dev:all
```
after this couple of command, server with json-server will be opened on host 3000 or 3001,<br>
and app will be running

Another one studying project =)